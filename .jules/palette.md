## 2024-08-01 - Focus Visible Native Elements
**Learning:** Native interactive elements like `<select>` must explicitly use `focus-visible:outline-none focus-visible:tac-focus-premium` for consistent custom focus rings and keyboard accessibility across the application.
**Action:** Always add these utility classes to native `<select>` or `<input>` elements.
