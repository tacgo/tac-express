## 2024-08-24 - Provide Descriptive ARIA Labels
**Learning:** Found that generic `aria-label` values like 'Cancel' do not provide sufficient context when multiple similar inputs appear in a list.
**Action:** Always provide descriptive `aria-label` text to icon buttons such as 'Cancel editing' instead of just 'Cancel', especially inside lists.
